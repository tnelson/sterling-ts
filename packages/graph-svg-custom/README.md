# Development Notes 

This package was modified on 26 June, 2025 to factor out the `graph-svg` package. The `graph-svg` package was only used in the project via imports from its `math/line.ts` file, so that file is now in this package (`graph-svg-custom`).